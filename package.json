{
  "private": true,
  "scripts": {
    "build:st": "wasm-pack build --target web --release",
    "build:pt": "wasm-pack build --target web --release --out-dir pkg-parallel -- --features parallel",
    "serve": "npm run build:st && npm run build:pt && webpack-dev-server "
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "html-webpack-plugin": "^5.5.3",
    "npm-run-all": "^4.1.5",
    "wasm-pack": "^0.12.1",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1",
    "webpack-dev-server": "^4.15.1"
  },
  "dependencies": {
    "comlink": "^4.3.1",
    "wasm-feature-detect": "^1.3.0",
    "wasm-parallel": "file:./pkg-parallel",
    "wasm-single": "file:./pkg"
  }
}
